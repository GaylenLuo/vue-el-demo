# .yarnrc.yml (放在项目根目录)
nodeLinker: "node-modules"  # 关键！改用传统 node_modules 链接器（避免 PnP 在 CI 中的问题）
npmRegistryServer: "https://registry.npmjs.org"
enableGlobalCache: true
# 以下两行是解决 YN0028 的核心：在 CI 环境中允许修改锁文件（CF Pages 会安全处理）
allowSandboxUnpluggedModules: true
